# API keys — stored at ~/.secrets (created automatically by install.sh)
# Sourced by ~/.zshrc
#
# Edit ~/.secrets with your actual keys after running install.sh

export FIRECRAWL_API_KEY=""
export CONTEXT7_API_KEY=""
export GITHUB_TOKEN=""
